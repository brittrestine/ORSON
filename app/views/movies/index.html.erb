<div class="movie-container">
	<h2>Now Playing</h2>
	<%= render partial: "movies/movies", locals: { movies: @movies } %>
</div>
<div class="container">
	<button class="movie-button" id="load-more-movies" data-page="2">Load more</button>
</div>